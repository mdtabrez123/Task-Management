# Copy this file to .env and fill in your real values (do NOT commit the real .env to git)

# Port the backend should listen on
PORT=5000

# MongoDB connection string
# Example (MongoDB Atlas):
# MONGO_URI="mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/your-db-name?retryWrites=true&w=majority"
MONGO_URI="your_mongo_connection_string_here"

# Secret used to sign JWT tokens. Keep this safe and private.
JWT_SECRET="your_jwt_secret_here"

# Optional: change CORS origin in server.js if your frontend runs on another origin
# CORS_ORIGIN=http://localhost:5173
